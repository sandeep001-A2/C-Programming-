#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int P;
    float R, T, x;
    scanf("%d %f %f", &P, &R, &T);
    x=P*R*T/100;
    printf("%.2f", x);
    return 0;
}
